namespace ChessGame.Core {
    public enum PieceType {
        pawn,
        bishop,
        knight,
        rook,
        queen,
        king
    }

    public enum Color {
        black,
        white
    }
}